# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Jekyll 3.8 runs on Ruby 2.6 in the repository's legacy CI image. Newer ffi
# releases require Ruby 3, so keep the last compatible line for reproducible builds.
gem "ffi", "~> 1.16.3"
